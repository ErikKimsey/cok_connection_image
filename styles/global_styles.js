import React, { Component } from 'react';
import { StyleSheet, View } from 'react-native';
import { Button } from 'native-base';

export default (GLOBAL_STYLES = {
	textColor: '#000000',
	highlightBlue: 'rgb(80,141,179)',
	backgroundColor: '#ffffff'
});
